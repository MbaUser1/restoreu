/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "7dzyhzftta6fepoj.public.blob.vercel-storage.com",
      "cdn.rareblocks.xyz",
    ],
  },
};

export default nextConfig;
